exp_name: jester_10_ver2
seed: 42

data:
  csv_path: data/meta.csv
  data_dir: data

model:
  in_channels: 3
  hidden_dim: 64
  num_layers: 3
  dropout: 0.5

train:
  epochs: 20
  batch_size: 32
  lr: 0.001
  weight_decay: 0.0001
  warmup_ratio: 0.1